# Billing

Keep billing-provider code, product/price mapping, webhook handlers, subscription state rules, and billing tests here.

Before implementation, complete the Billing section of `../docs/PROJECT-BRIEF.md`. Development must use test-mode credentials until the complete billing lifecycle is verified.
