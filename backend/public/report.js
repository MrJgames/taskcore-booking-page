(function(){"use strict";const match=location.pathname.match(/^\/report\/([^/]+)/);if(!match)return;const token=match[1];document.querySelectorAll("form[data-finding]").forEach(form=>form.addEventListener("submit",async event=>{event.preventDefault();const button=event.submitter;const output=form.querySelector("output");output.textContent="Saving…";try{const response=await fetch(`/api/reports/${encodeURIComponent(token)}/findings/${encodeURIComponent(form.dataset.finding)}/decision`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({decision:button.value,comment:form.querySelector("textarea").value})});const data=await response.json();output.textContent=response.ok?data.decision:(data.error||"Could not save.")}catch{output.textContent="Could not save. Please try again."}}))}());
